import test from "node:test"; import assert from "node:assert/strict"; import { serializeCateringPackage } from "./catering-package";
test("package serializer converts decimals and timestamps", () => { const date = new Date("2026-01-02T00:00:00.000Z"); const result = serializeCateringPackage({ id: "id", providerId: "p", title: "Title", description: "Description long enough here", category: "Other", coverImage: null, galleryImages: [], pricingModel: "flat_rate", startingPrice: "125.50", currency: "USD", minimumGuests: 1, maximumGuests: null, preparationStyle: null, serviceStyle: null, cuisines: [], dietaryAccommodations: [], includedServices: [], optionalAddOns: [], estimatedDuration: null, active: true, featured: false, displayOrder: 0, createdAt: date, updatedAt: date }); assert.equal(result.startingPrice, 125.5); assert.equal(result.createdAt, date.toISOString()); });
